<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>1.460</version>
        <!-- which version of Jenkins is this plugin built against? -->
    </parent>

    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>docker-cloud</artifactId>
    <version>0.1-SNAPSHOT</version>
    <packaging>hpi</packaging>
    <name>Jenkins Docker Cloud plugin</name>
    <description>This plugin allows Jenkins to use local and remote docker containers as build environment.</description>
    <url>https://wiki.jenkins-ci.org/display/JENKINS/Docker+Cloud+Plugin</url>


    <developers>
        <developer>
            <id>simonswine</id>
            <name>Christian Simon</name>
            <email>simon@swine.de</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/simonswine/todo.git</connection>
        <developerConnection>scm:git:git@github.com:jenkinsci/simonswine/todo.git</developerConnection>
        <url>https://github.com/simonswine/todo</url>
    </scm>

    <!-- get every artifact through repo.jenkins-ci.org, which proxies all the artifacts that we need -->
    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

</project>
